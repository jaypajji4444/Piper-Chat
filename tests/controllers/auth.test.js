const { reqInvite, sendInvite } = require('../../controllers/auth');

describe('Invites', ()=>{
    describe('User should be able to send email for invitation', ()=>{
        it('Send a mail without error', (done)=>{ 
        })
    })
})